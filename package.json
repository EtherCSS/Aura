{
  "name": "aura-ai-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "A modern voice-enabled AI assistant powered by GitHub Models.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
